{
  "name": "rewrite-imports",
  "version": "1.0.0",
  "repository": "lukeed/rewrite-imports",
  "description": "Rewrite `import` statements as `require()`s; via RegExp",
  "license": "MIT",
  "author": {
    "name": "Luke Edwards",
    "email": "luke.edwards05@gmail.com",
    "url": "lukeed.com"
  },
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "test": "tape test/*.js | tap-spec",
    "prepublishOnly": "microbundle --format cjs --no-sourcemap --no-compress"
  },
  "main": "index.js",
  "browser": "dist/rewrite-imports.js",
  "files": [
    "index.js",
    "dist"
  ],
  "keywords": [
    "regex",
    "rewrite",
    "modules",
    "imports",
    "requires",
    "aliases"
  ],
  "devDependencies": {
    "microbundle": "^0.6.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.7.0"
  }
}
